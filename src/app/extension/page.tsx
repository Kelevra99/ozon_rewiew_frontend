import type { Metadata } from 'next';
import { LegalPageLayout } from '@/components/site/legal-page-layout';

export const metadata: Metadata = {
  title: 'Как работает браузерное расширение',
  description: 'Описание работы браузерного расширения KaiRox для генерации ответов на отзывы на маркетплейсах.',
};

function Section({
  title,
  children,
}: {
  title: string;
  children: React.ReactNode;
}) {
  return (
    <section className="space-y-3">
      <h2 className="text-2xl font-semibold text-white">{title}</h2>
      <div className="space-y-3 text-slate-200">{children}</div>
    </section>
  );
}

function QuoteCard({
  title,
  text,
}: {
  title: string;
  text: string;
}) {
  return (
    <div className="rounded-2xl border border-white/10 bg-slate-950/30 p-5">
      <div className="text-lg font-semibold text-white">{title}</div>
      <div className="mt-3 text-sm leading-7 text-slate-200 whitespace-pre-line">{text}</div>
    </div>
  );
}

export default function ExtensionPage() {
  return (
    <LegalPageLayout
      title="Как работает браузерное расширение"
      description="Здесь описано, какие действия выполняет расширение KaiRox на странице маркетплейса и какие данные используются для генерации ответа."
      updatedAt="31.03.2026"
    >
      <Section title="Общее описание">
        <p>
          Браузерное расширение KaiRox помогает отвечать на отзывы непосредственно на странице маркетплейса.
          Оно добавляет на страницу собственные кнопки и элементы интерфейса, считывает данные из DOM-структуры
          страницы, передает необходимые сведения на сервер KaiRox и вставляет сгенерированный ответ обратно в форму ответа.
        </p>
      </Section>

      <Section title="Какие данные расширение может считывать">
        <p>
          Расширение может считывать текст отзыва, оценку, название товара, артикул, описание товара,
          сведения о текущем ответе продавца, а также иные данные, присутствующие в DOM страницы и
          необходимые для формирования ответа.
        </p>
      </Section>

      <Section title="Что расширение делает на странице">
        <p>— считывает DOM-структуру страницы;</p>
        <p>— находит карточки отзывов и поля ответа;</p>
        <p>— добавляет собственные кнопки и элементы интерфейса KaiRox;</p>
        <p>— отправляет на сервер данные, необходимые для генерации ответа;</p>
        <p>— получает сгенерированный текст и вставляет его в интерфейс маркетплейса.</p>
      </Section>

      <Section title="Чего расширение не требует">
        <p>
          Для ручного и автоматического режима через расширение не требуется передавать KaiRox логин и пароль
          от кабинета маркетплейса, создавать отдельного менеджера или выдавать дополнительные права доступа.
        </p>
      </Section>

      <Section title="API-ключ">
        <p>
          Для работы расширения используется API-ключ пользователя KaiRox. Все действия, совершенные с этим ключом,
          и все списания с баланса считаются действиями пользователя, которому принадлежит ключ.
        </p>
      </Section>

      <Section title="Текст для страницы установки расширения">
        <QuoteCard
          title="Готовый текст"
          text={`Расширение KaiRox помогает отвечать на отзывы прямо на странице маркетплейса. Оно считывает данные отзыва из DOM страницы, отправляет их на сервер KaiRox для генерации ответа и вставляет результат обратно в форму ответа. Расширение не требует передачи логина и пароля от кабинета маркетплейса, а работает через API-ключ пользователя KaiRox.`}
        />
      </Section>

      <Section title="Короткий текст для карточки расширения">
        <QuoteCard
          title="Готовый короткий текст"
          text={`KaiRox — расширение для генерации ответов на отзывы на маркетплейсах. Считывает данные отзыва со страницы, отправляет их в KaiRox и вставляет готовый ответ прямо в форму ответа.`}
        />
      </Section>
    </LegalPageLayout>
  );
}
